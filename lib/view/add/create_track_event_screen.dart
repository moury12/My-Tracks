import 'package:flutter/material.dart';

class CreateTrackEventScreen extends StatelessWidget {
  const CreateTrackEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(
        child: Text('CreateTrackEvent'),

    );
  }
}